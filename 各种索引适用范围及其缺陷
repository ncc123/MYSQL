B-TREE索引是有序的，按照创建索引的列顺序比较。
适用范围：
1.索引列全部匹配。
2.最左列匹配。
3.列前缀匹配。  like 'B%'  如果使用这一条需要注意一点，如果是多列组成的索引，在索引的中间列like，like之后的列不在走索引。最好只用于最左列单个条件
4.最走列范围匹配。between and
5.精确匹配一列并且范围匹配一列。记得最左匹配，索引列顺序。
6.B-tree是有序的，如果是匹配条件满足索引，order by也是其中一列，也是走索引的。
